function Move(fromRow, fromCol, toRow, toCol) {
  console.log("Move object created: fromRow: " + fromRow + " fromCol: " +
    fromCol + " toRow: " + toRow + " toCol: " + toCol);
  fromRow = fromRow;
  fromCol = fromCol;
  toRow = toRow;
  toCol = toCol;
}
